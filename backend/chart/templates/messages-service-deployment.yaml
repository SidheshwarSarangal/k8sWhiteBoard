apiVersion: apps/v1
kind: Deployment
metadata:
  name: messages-service
  namespace: {{ .Values.namespace }}
  labels:
    app: messages-service
spec:
  replicas: {{ .Values.messages.replicas }}
  selector:
    matchLabels:
      app: messages-service
  template:
    metadata:
      labels:
        app: messages-service
    spec:
      containers:
        - name: messages-service
          image: "{{ .Values.messages.image }}:{{ .Values.messages.tag }}"
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: {{ .Values.messages.port }}
          env:
            - name: PORT
              value: "{{ .Values.messages.port | toString }}"
            - name: MONGO_URI
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secretName }}
                  key: MONGO_URI
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secretName }}
                  key: JWT_SECRET
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.messages.port }}
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.messages.port }}
            initialDelaySeconds: 5
            periodSeconds: 5
