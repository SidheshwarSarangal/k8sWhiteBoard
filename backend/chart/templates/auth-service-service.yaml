apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: {{ .Values.backendNamespace }}
  labels:
    app: auth-service
spec:
  type: ClusterIP
  selector:
    app: auth-service
  ports:
    - port: {{ .Values.auth.port }}
      targetPort: {{ .Values.auth.port }}
      protocol: TCP
      name: http
