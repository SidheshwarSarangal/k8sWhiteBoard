apiVersion: v1
kind: Service
metadata:
  name: realtime-service
  namespace: whiteboard
  labels:
    app: realtime-service
spec:
  type: ClusterIP
  selector:
    app: realtime-service
  ports:
    - port: 3004
      targetPort: 3004
      protocol: TCP
      name: http
